apiVersion: v1
kind: Pod
metadata:
  name: elasticsearch
spec:
  containers:
     - name: elasticsearch
       image: "docker.elastic.co/elasticsearch/elasticsearch"
       ports:
         - containerPort: 9200
       imagePullPolicy: Always
